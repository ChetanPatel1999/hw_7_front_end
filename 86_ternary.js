//wap to check given alpha is vovel or consonent.
let alpha;
alpha = prompt("enter a alpha : ");
(alpha == 'a' || alpha == 'e' || alpha == 'i' || alpha == 'o' || alpha == 'u')
    ? document.write("alpha is vovel") : document.write("alpha is consonant");